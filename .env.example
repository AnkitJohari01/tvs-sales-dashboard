# Copy to .env and fill in. Never commit the real .env.
# Anthropic key used ONLY by the /explain-alert endpoint (optional; falls back to a
# rule-based explanation if unset).
ANTHROPIC_API_KEY=

# Comma-separated allowed CORS origins for the frontend (optional).
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
